import * as React from 'react';

/**
 * A base container for the entire application.
 */
export class App extends React.PureComponent {
  public render(): React.ReactNode {
    return (
      <p>Hello, Sydoky!</p>
    );
  }
}

export default App;
